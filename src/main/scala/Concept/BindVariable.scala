package Concept


object BindVariable {




  def main(args: Array[String]): Unit = {
    val array=(1,2,3,4,5,6,7,8)
    val stack = (1,23,4)
    var queue = (1,2,3,4)

//    array match {
//      case mutable.Stack(head, tails @ _*) => (head, tails)
//    }
//
//    stack match {
//      case mutable.Stack(head, tails @ _*) => (head, tails)
//    }
//
//    queue match {
//      case mutable.Stack(head, tails @ _*) => (head, tails)
//    }
  }
}
